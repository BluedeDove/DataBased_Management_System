# 项目说明

> 本文件用于给当前仓库做“结构说明 + 归类说明”，方便后续整理，但**不代表应立即删除任何文件**。  
> 本仓库同时保留了**当前浏览器版（B/S）主线**与**旧 Electron 桌面版参考代码**，两套代码不能简单按“重复”处理。  
> `项目说明.md`：本文件自身，作用是汇总项目结构、文件分类、当前主线、历史参考线、脚本、文档、构建产物和本地状态文件，供后续归档整理时查阅。

## 1. 一句话结论

- **当前运行主线**：`web/` + `server/` + `data/library.db`
- **旧桌面版参考主线**：`src/` + 根目录 `index.html` + `start-dev.bat`
- **文档/比赛材料主线**：`docs/`、`plans/`、报告 `pdf/docx`、界面截图 `pic/`
- **构建产物/本地状态**：`dist/`、`dist-renderer/`、`dist-electron/`、`logs/`、`.vs/`、`.claude/`

---

## 2. 归类规则

| 分类 | 含义 | 处理建议 |
|---|---|---|
| 当前主线 | 现在实际用于浏览器版运行的代码和配置 | 优先维护 |
| 历史参考 | 旧 Electron 桌面版实现，仍可作为迁移参考 | 先保留 |
| 文档材料 | 比赛、课程、规划、分析、说明文档 | 先保留 |
| 脚本工具 | 安装、启动、数据生成、报告导出等脚本 | 按用途保留 |
| 构建产物 | 构建后的静态文件、编译产物 | 可归档，但先别动 |
| 本地状态 | IDE/AI/运行日志/依赖目录等 | 不属于业务源码 |

---

## 3. 根目录总览

### 3.1 根目录子目录

| 路径 | 分类 | 当前状态 | 作用说明 |
|---|---|---|---|
| `.claude/` | 本地工具配置 | 本地状态 | Claude/AI 工具本地权限与行为配置目录 |
| `.git/` | Git 元数据 | 本地状态 | 版本控制目录 |
| `.vs/` | IDE 状态 | 本地状态 | Visual Studio 工作区状态、布局、缓存 |
| `backup/` | 备份代码 | 历史参考 | 一些重构尝试的备份文件 |
| `data/` | 数据目录 | 当前主线 | 当前 B/S 版 SQLite 数据库所在目录 |
| `dist/` | 构建产物 | 产物 | 当前 Web 前端构建输出目录 |
| `dist-electron/` | 构建产物 | 历史产物 | 旧 Electron 主进程/预加载构建输出 |
| `dist-renderer/` | 构建产物 | 历史产物 | 旧 Electron 渲染端构建输出 |
| `docs/` | 项目文档 | 文档材料 | 比赛/课程提交文档的 Markdown 成稿 |
| `logs/` | 运行日志 | 本地状态 | 后端运行审计日志等 |
| `node_modules/` | 依赖目录 | 本地状态 | 已安装依赖 |
| `pic/` | 截图素材 | 文档材料 | 项目界面展示截图 |
| `plans/` | 规划分析 | 文档材料 | 各阶段 AI/人工整理出的分析与改造计划 |
| `public/` | 静态资源 | 当前主线 | 当前 Web 版公共资源 |
| `scripts/` | 脚本工具 | 工具 | 数据生成、清库、报告导出等脚本 |
| `server/` | 后端代码 | 当前主线 | Express + SQLite 后端 |
| `src/` | 旧 Electron 代码 | 历史参考 | 旧桌面版主进程/预加载/渲染端实现 |
| `tests/` | 测试代码 | 历史参考偏多 | 当前测试主要指向旧 Electron 代码 |
| `web/` | 前端代码 | 当前主线 | Vue 3 + Vite 浏览器前端 |

### 3.2 根目录文件

| 路径 | 分类 | 当前状态 | 作用说明 |
|---|---|---|---|
| `.env.example` | 配置模板 | 通用 | AI 接口等环境变量示例 |
| `.eslintrc.cjs` | 工具配置 | 通用 | ESLint 规则配置 |
| `.gitignore` | 工具配置 | 通用 | Git 忽略规则，内容兼顾 Node、Electron、数据库、文档等 |
| `.npmrc` | 工具配置 | 历史兼容 | npm 镜像配置，同时保留 Electron 下载镜像设置 |
| `.nvmrc` | 工具配置 | 当前有效 | 指定 Node 版本 20 |
| `.replit` | 平台配置 | 当前主线辅助 | Replit 运行和部署配置 |
| `$null` | 临时/异常文件 | 待确认 | 看起来像误生成文件，先不要动 |
| `01-2 作品信息摘要模板（2025版）V2.docx` | 文档模板 | 文档材料 | 比赛/申报模板原件 |
| `01-3 软件应用与开发类作品设计和开发文档模板（2025版）.docx` | 文档模板 | 文档材料 | 比赛/申报模板原件 |
| `CLAUDE.md` | AI 协作说明 | 文档材料 | 给 AI 编码助手的仓库说明，当前 `web/ + server/` 描述较清楚 |
| `database-course-design-report.pdf` | 成品文档 | 文档材料 | 课程/比赛成品报告 PDF |
| `deploy.bat` | 启动/部署脚本 | 当前主线辅助 | 构建前端并启动后端的部署辅助脚本 |
| `DEPLOYMENT.md` | 部署说明 | 当前主线说明 | 当前 B/S 版启动和部署说明 |
| `index.html` | 历史入口 | 历史参考 | 旧 Electron 渲染端入口，加载 `src/renderer/src/main.ts` |
| `install.ps1` | 安装脚本 | 通用偏历史 | Windows 安装脚本，仍带 Electron 镜像配置痕迹 |
| `install.sh` | 安装脚本 | 通用偏历史 | Linux/Mac 安装脚本，仍带 Electron 镜像配置痕迹 |
| `LICENSE` | 许可证 | 通用 | 开源许可证 |
| `ngrok.exe` | 第三方工具 | 演示辅助 | ngrok 本地二进制 |
| `ngrok.yml` | 第三方工具配置 | 演示辅助 | ngrok 配置文件，用于公网分享本地服务 |
| `nsis-3.0.4.1.7z` | 打包工具压缩包 | 历史参考 | Electron 安装包相关工具 |
| `package-lock.json` | 依赖锁文件 | 当前有效 | 根依赖锁文件 |
| `package.json` | 项目总控 | 当前主线 | 根脚本入口，负责前后端联动、数据脚本、Replit 脚本 |
| `pic.7z` | 素材压缩包 | 文档材料 | 界面截图素材压缩备份 |
| `README.md` | 项目说明 | 文档材料 | 总体介绍，但内容存在新旧架构混合痕迹 |
| `replit.nix` | 平台配置 | 当前主线辅助 | Replit 环境依赖声明 |
| `setup_and_run.ps1` | 历史脚本 | 历史参考 | 旧 Electron 版安装并运行脚本 |
| `share.bat` | 演示脚本 | 当前主线辅助 | 启动 ngrok 对外分享当前 Web 服务 |
| `start-dev.bat` | 历史脚本 | 历史参考 | 旧 Electron 开发脚本 |
| `start.bat` | 启动脚本 | 当前主线 | Windows 下一键启动后端和前端 |
| `start.sh` | 启动脚本 | 当前主线 | Linux/Mac 下一键启动后端和前端 |
| `TEST_ACCOUNTS.txt` | 本地便签 | 本地说明 | 测试账号说明，末尾混有 AI 配置信息，属于敏感本地说明文件 |
| `tsconfig.json` | 工具配置 | 当前有效 | 根 TypeScript 配置，覆盖 `server/src` 与 `scripts` |
| `tsconfig.node.json` | 工具配置 | 当前有效 | Node/Vite 辅助 TS 配置 |
| `vitest.config.ts` | 测试配置 | 历史参考偏多 | 测试别名指向旧 Electron 代码路径 |
| `winCodeSign-2.6.0.7z` | 打包工具压缩包 | 历史参考 | Electron 签名相关工具 |
| `项目说明.md` | 说明文档 | 当前新增说明 | 本文件，汇总仓库结构、归类结果、主线与历史参考线 |

---

## 4. 当前运行主线：浏览器版（B/S）

### 4.1 当前主线判定

- 当前浏览器版前端入口：`web/index.html`
- 当前浏览器版前端源码：`web/src/`
- 当前浏览器版后端源码：`server/src/`
- 当前浏览器版数据库：`data/library.db`
- 当前浏览器版默认启动方式：`start.bat` / `start.sh`

### 4.2 当前前端：`web/`

#### 4.2.1 前端顶层文件

| 路径 | 作用说明 |
|---|---|
| `web/package.json` | 当前前端子包定义 |
| `web/tsconfig.json` | 当前前端 TypeScript 配置 |
| `web/vite.config.ts` | Vite 配置，开发端口 `3000`，并代理 `/api -> 3001` |
| `web/index.html` | 当前浏览器版前端真实入口 |

#### 4.2.2 前端入口与基础文件

| 路径 | 作用说明 |
|---|---|
| `web/src/main.ts` | Vue 应用启动入口 |
| `web/src/App.vue` | 应用根组件 |
| `web/src/env.d.ts` | Vite 环境类型声明 |
| `web/src/router/index.ts` | 当前路由配置，已接入 `Notes` 页面 |
| `web/src/store/user.ts` | 当前登录态、Token、用户信息管理 |
| `web/src/components/Layout.vue` | 当前整体布局、侧边导航、顶部栏 |
| `web/src/styles/index.css` | 当前全局视觉样式与设计系统 |

#### 4.2.3 前端 API 层

| 路径 | 作用说明 |
|---|---|
| `web/src/api/index.ts` | Axios 基座、请求封装、Token 自动刷新、统一响应类型 |
| `web/src/api/auth.api.ts` | 登录、注册、校验 Token、权限相关接口 |
| `web/src/api/book.api.ts` | 图书、图书分类、搜索、统计接口 |
| `web/src/api/reader.api.ts` | 读者、读者类别、挂失/续期/统计接口 |
| `web/src/api/borrowing.api.ts` | 借书、还书、续借、借阅统计接口 |
| `web/src/api/ai.api.ts` | AI 聊天、SSE 流式响应、向量生成、会话接口 |
| `web/src/api/other.api.ts` | AI 配置、SQL 搜索、导出等杂项接口 |
| `web/src/api/notes.api.ts` | 读书笔记/传承笔记接口 |

#### 4.2.4 前端工具文件

| 路径 | 作用说明 |
|---|---|
| `web/src/utils/debounceSubmit.ts` | 防重复提交工具 |
| `web/src/utils/conflictHandler.ts` | 409 冲突提示与版本冲突处理工具 |

#### 4.2.5 前端页面

| 路径 | 作用说明 |
|---|---|
| `web/src/views/Login.vue` | 登录页 |
| `web/src/views/Register.vue` | 注册页 |
| `web/src/views/Dashboard.vue` | 控制台首页 |
| `web/src/views/Books.vue` | 图书管理页 |
| `web/src/views/Readers.vue` | 读者管理页 |
| `web/src/views/Borrowing.vue` | 借阅/归还/续借页 |
| `web/src/views/Statistics.vue` | 统计分析页 |
| `web/src/views/AIAssistant.vue` | AI 助手页 |
| `web/src/views/Settings.vue` | 系统设置页 |
| `web/src/views/Notes.vue` | 读书笔记/传承长廊功能页，属于当前 Web 版有效新功能 |

#### 4.2.6 前端源码目录内的编译副产物

- `web/src/**/*.js`
- `web/src/**/*.js.map`

这些文件基本是 `ts/vue` 文件旁边的编译副产物/SourceMap，**不是主要源码**。  
它们最像“后续可归档候选”，但因为目前多人协作，先不要动。

### 4.3 当前后端：`server/`

#### 4.3.1 后端顶层文件

| 路径 | 作用说明 |
|---|---|
| `server/package.json` | 当前后端子包定义 |
| `server/tsconfig.json` | 后端 TypeScript 编译配置 |

#### 4.3.2 后端入口与基础装配

| 路径 | 作用说明 |
|---|---|
| `server/src/server.ts` | 后端启动入口，负责初始化数据库、自动种子、初始化 AI |
| `server/src/app.ts` | Express 应用装配，挂中间件、路由、健康检查、静态托管 |
| `server/src/config/index.ts` | 后端配置中心 |
| `server/src/database/index.ts` | SQLite 数据库初始化、建表、迁移、默认数据 |
| `server/src/database/health-check.ts` | 数据库结构健康检查 |

#### 4.3.3 后端路由层

| 路径 | 作用说明 |
|---|---|
| `server/src/routes/auth.routes.ts` | 认证路由 |
| `server/src/routes/book.routes.ts` | 图书与分类路由 |
| `server/src/routes/reader.routes.ts` | 读者与读者类别路由 |
| `server/src/routes/borrowing.routes.ts` | 借阅路由 |
| `server/src/routes/ai.routes.ts` | AI、SSE、向量检索、工具调用路由 |
| `server/src/routes/config.routes.ts` | AI 配置读写与测试路由 |
| `server/src/routes/search.routes.ts` | SQL 搜索相关路由 |
| `server/src/routes/export.routes.ts` | 导出 CSV/JSON/报告路由 |

#### 4.3.4 后端控制器层

| 路径 | 作用说明 |
|---|---|
| `server/src/controllers/auth.controller.ts` | 认证控制器 |
| `server/src/controllers/book.controller.ts` | 图书控制器 |
| `server/src/controllers/reader.controller.ts` | 读者控制器 |
| `server/src/controllers/borrowing.controller.ts` | 借阅控制器 |

#### 4.3.5 后端中间件

| 路径 | 作用说明 |
|---|---|
| `server/src/middleware/auth.middleware.ts` | JWT 鉴权中间件 |
| `server/src/middleware/permission.middleware.ts` | 权限校验中间件 |
| `server/src/middleware/rateLimit.middleware.ts` | 限流中间件 |
| `server/src/middleware/error.middleware.ts` | 错误处理中间件 |
| `server/src/middleware/validation.middleware.ts` | 请求参数校验中间件 |
| `server/src/middleware/audit.middleware.ts` | 审计日志中间件 |

#### 4.3.6 后端公共库

| 路径 | 作用说明 |
|---|---|
| `server/src/lib/logger.ts` | 日志工具 |
| `server/src/lib/jwt.ts` | JWT 生成、验证、刷新辅助 |
| `server/src/lib/exportService.ts` | 导出服务 |
| `server/src/lib/errorHandler.ts` | 统一错误类型与错误封装 |
| `server/src/lib/autoSeed.ts` | 空库自动种子数据逻辑 |

#### 4.3.7 后端领域层：认证

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/auth/auth.service.ts` | 登录、注册、权限、密码变更等业务逻辑 |
| `server/src/domains/auth/user.repository.ts` | 用户数据访问层 |

#### 4.3.8 后端领域层：图书

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/book/book.service.ts` | 图书业务逻辑 |
| `server/src/domains/book/book.repository.ts` | 图书数据访问层 |

#### 4.3.9 后端领域层：读者

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/reader/reader.service.ts` | 读者业务逻辑 |
| `server/src/domains/reader/reader.repository.ts` | 读者数据访问层 |

#### 4.3.10 后端领域层：借阅

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/borrowing/borrowing.service.ts` | 借书、还书、续借、赔偿等业务逻辑 |
| `server/src/domains/borrowing/borrowing.repository.ts` | 借阅数据访问层 |

#### 4.3.11 后端领域层：搜索

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/search/sql-search.service.ts` | 受控 SQL 查询服务 |
| `server/src/domains/search/regex-search.service.ts` | 正则/模糊搜索服务 |

#### 4.3.12 后端领域层：AI

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/ai/ai.service.ts` | AI 配置读取、语义搜索、聊天上下文等服务 |
| `server/src/domains/ai/tools.ts` | AI 工具调用定义与执行逻辑 |

#### 4.3.13 后端领域层：读书笔记 / 传承笔记

| 路径 | 作用说明 |
|---|---|
| `server/src/domains/note/note.routes.ts` | 笔记功能路由 |
| `server/src/domains/note/note.controller.ts` | 笔记控制器 |
| `server/src/domains/note/note.service.ts` | 笔记业务逻辑 |
| `server/src/domains/note/note.repository.ts` | 笔记数据访问层 |

> 这一整套 `note` 相关代码是**当前 Web 版有效功能**，不是临时样例。

---

## 5. 旧 Electron 主线（保留参考）

### 5.1 如何识别旧主线

- 根目录 `index.html` 指向 `src/renderer/src/main.ts`
- `src/main/index.ts` 使用 `electron`
- `src/preload/index.ts` 暴露 `window.api`
- `start-dev.bat`、`setup_and_run.ps1` 明显按 Electron 流程启动
- `dist-renderer/`、`dist-electron/` 是这套主线的构建产物

### 5.2 旧 Electron 顶层入口

| 路径 | 作用说明 |
|---|---|
| `index.html` | 旧 Electron 渲染端入口 HTML |
| `src/main/index.ts` | Electron 主进程入口 |
| `src/preload/index.ts` | Electron preload，向渲染端暴露 IPC API |

### 5.3 旧 Electron 渲染端

| 路径 | 作用说明 |
|---|---|
| `src/renderer/src/main.ts` | 旧渲染端 Vue 启动入口 |
| `src/renderer/src/App.vue` | 旧渲染端根组件 |
| `src/renderer/src/env.d.ts` | 旧渲染端环境声明 |
| `src/renderer/src/router/index.ts` | 旧渲染端路由 |
| `src/renderer/src/store/user.ts` | 旧渲染端用户状态 |
| `src/renderer/src/components/Layout.vue` | 旧渲染端主布局 |
| `src/renderer/src/styles/index.css` | 旧渲染端全局样式 |
| `src/renderer/src/utils/debounceSubmit.ts` | 旧渲染端工具函数 |

#### 旧渲染端页面

| 路径 | 作用说明 |
|---|---|
| `src/renderer/src/views/Login.vue` | 旧登录页 |
| `src/renderer/src/views/Register.vue` | 旧注册页 |
| `src/renderer/src/views/Dashboard.vue` | 旧控制台页 |
| `src/renderer/src/views/Books.vue` | 旧图书管理页 |
| `src/renderer/src/views/Readers.vue` | 旧读者管理页 |
| `src/renderer/src/views/Borrowing.vue` | 旧借阅页 |
| `src/renderer/src/views/Borrowing-backup.vue` | 旧借阅页备份快照 |
| `src/renderer/src/views/Statistics.vue` | 旧统计页 |
| `src/renderer/src/views/AIAssistant.vue` | 旧 AI 助手页 |
| `src/renderer/src/views/Settings.vue` | 旧设置页 |

### 5.4 旧 Electron 主进程领域与基础设施

#### 旧配置 / 数据库

| 路径 | 作用说明 |
|---|---|
| `src/main/config/index.ts` | 旧桌面版配置中心 |
| `src/main/database/index.ts` | 旧桌面版数据库初始化与迁移 |
| `src/main/database/health-check.ts` | 旧桌面版数据库健康检查 |

#### 旧基础库

| 路径 | 作用说明 |
|---|---|
| `src/main/lib/logger.ts` | 日志工具 |
| `src/main/lib/errorHandler.ts` | 错误类型封装 |
| `src/main/lib/exportService.ts` | 桌面版导出服务，输出到系统下载目录 |
| `src/main/lib/ipcHandlers.ts` | IPC 处理器注册，桌面版核心桥接层 |
| `src/main/lib/auditLogger.ts` | 审计日志工具 |
| `src/main/lib/operationLogger.ts` | 操作日志工具 |
| `src/main/lib/optimisticLock.ts` | 乐观锁辅助 |
| `src/main/lib/permissions.ts` | 权限辅助 |
| `src/main/lib/retryHandler.ts` | 重试处理工具 |
| `src/main/lib/softDelete.ts` | 软删除辅助 |

#### 旧领域代码

| 路径 | 作用说明 |
|---|---|
| `src/main/domains/auth/auth.service.ts` | 旧认证服务 |
| `src/main/domains/auth/user.repository.ts` | 旧用户仓储 |
| `src/main/domains/book/book.service.ts` | 旧图书服务 |
| `src/main/domains/book/book.repository.ts` | 旧图书仓储 |
| `src/main/domains/reader/reader.service.ts` | 旧读者服务 |
| `src/main/domains/reader/reader.repository.ts` | 旧读者仓储 |
| `src/main/domains/borrowing/borrowing.service.ts` | 旧借阅服务 |
| `src/main/domains/borrowing/borrowing.repository.ts` | 旧借阅仓储 |
| `src/main/domains/search/sql-search.service.ts` | 旧 SQL 搜索服务 |
| `src/main/domains/search/regex-search.service.ts` | 旧正则搜索服务 |
| `src/main/domains/search/index.ts` | 旧搜索域导出 |
| `src/main/domains/ai/ai.service.ts` | 旧 AI 服务 |
| `src/main/domains/ai/conversation.repository.ts` | 旧 AI 会话仓储 |
| `src/main/domains/ai/embedding.service.ts` | 旧向量嵌入服务 |
| `src/main/domains/ai/vector.repository.ts` | 旧向量仓储 |
| `src/main/domains/config/config.service.ts` | 旧配置服务 |
| `src/main/domains/config/config.repository.ts` | 旧配置仓储 |

> 整个 `src/` 目录建议统一标记为：**旧 Electron 主线 / 历史参考实现**。  
> 虽然与当前 `web/ + server/` 存在功能重叠，但不能简单当作垃圾代码。

---

## 6. 脚本、测试、备份

### 6.1 数据与维护脚本：`scripts/`

#### 当前更贴近 Web 版的脚本

| 路径 | 作用说明 |
|---|---|
| `scripts/generate-data-v2.ts` | 当前更贴近 B/S 版的数据生成脚本，直接面向根目录 `data/library.db` |

#### 明显偏旧 Electron 路线的脚本

| 路径 | 作用说明 |
|---|---|
| `scripts/generate-data.ts` | 旧数据生成脚本，按 Electron `APPDATA` 路径找数据库 |
| `scripts/clear-database.ts` | 旧清库脚本，也按 Electron 用户目录定位数据库 |

#### 报告/PDF 生成脚本

| 路径 | 作用说明 |
|---|---|
| `scripts/generate-pdf.ts` | 使用 Puppeteer 生成课程报告 PDF 的脚本 |
| `scripts/generate-pdf-pandoc.ts` | 使用 Pandoc + Mermaid 生成课程报告 PDF 的脚本 |
| `scripts/generate-pdf-pandoc.ps1` | PowerShell 版 Pandoc 报告导出脚本 |
| `scripts/generate-pdf-script.md` | PDF 生成脚本草稿/说明文档 |

#### 归档脚本

| 路径 | 作用说明 |
|---|---|
| `scripts/archive/generateTestData.ts` | 旧版测试数据生成脚本归档 |
| `scripts/archive/generateTestData.js` | 上述脚本的旧编译产物 |

#### 数据素材

| 路径 | 作用说明 |
|---|---|
| `scripts/data/books-data.ts` | 图书样本数据 |
| `scripts/data/chinese-names.ts` | 中文姓名素材 |
| `scripts/data/publishers.ts` | 出版社素材 |
| `scripts/data/universities.ts` | 学校/机构素材 |

#### 生成器拆分文件

| 路径 | 作用说明 |
|---|---|
| `scripts/generators/book.generator.ts` | 图书数据生成器 |
| `scripts/generators/reader.generator.ts` | 读者数据生成器 |
| `scripts/generators/borrowing.generator.ts` | 借阅记录生成器 |
| `scripts/generators/edge-cases.generator.ts` | 边界测试数据生成器 |

#### 脚本工具函数

| 路径 | 作用说明 |
|---|---|
| `scripts/utils/random-utils.ts` | 随机数工具 |
| `scripts/utils/phone-generator.ts` | 手机号生成工具 |
| `scripts/utils/id-generator.ts` | 编号/证件号生成工具 |

### 6.2 测试：`tests/`

| 路径 | 作用说明 |
|---|---|
| `tests/setup.ts` | Vitest 初始化，mock 了 Electron `window.api` |
| `tests/services/borrowing.service.spec.ts` | 测试旧 Electron 路线的 `src/main/domains/borrowing/borrowing.service.ts` |

> 当前测试体系明显更偏向**旧 Electron 主线**，不是纯粹针对 `server/` 的现行后端测试。

### 6.3 备份：`backup/`

| 路径 | 作用说明 |
|---|---|
| `backup/enhanced-refactor/book.repository.ts` | 图书仓储重构备份稿 |
| `backup/enhanced-refactor/book.service.ts` | 图书服务重构备份稿 |
| `backup/enhanced-refactor/borrowing.service.ts` | 借阅服务重构备份稿 |

---

## 7. 文档、比赛材料、AI 规划文档

### 7.1 比赛/课程成稿：`docs/`

| 路径 | 作用说明 |
|---|---|
| `docs/01-2-作品信息概要表.md` | 比赛信息摘要表 Markdown 成稿 |
| `docs/01-3-设计开发文档.md` | 设计开发文档 Markdown 成稿 |

### 7.2 规划与分析：`plans/`

#### AI 功能类

| 路径 | 主题说明 |
|---|---|
| `plans/ai-assistant-conversation-fix-plan.md` | AI 对话保存与上下文传递问题 |
| `plans/ai-assistant-features-enhancement-plan.md` | AI 助手功能增强 |
| `plans/ai-assistant-redesign-plan.md` | AI 助手页面重设计 |
| `plans/ai-conversation-save-fix-plan.md` | AI 对话历史保存修复 |

#### 图书 / 借阅 / 业务问题类

| 路径 | 主题说明 |
|---|---|
| `plans/book-management-features-plan.md` | 图书管理功能增强 |
| `plans/borrowing-fix-plan.md` | 借阅功能修复 |
| `plans/borrowing-search-enhancement-plan.md` | 借阅搜索增强 |
| `plans/data-synchronization-fix-plan.md` | 数据结构同步问题修复 |
| `plans/multi-issue-fix-plan.md` | 多问题集中修复 |
| `plans/sql-search-fix-plan.md` | SQL 搜索模块修复 |
| `plans/soft-delete-fix-plan.md` | 软删除与唯一约束问题修复 |

#### 数据库 / 架构 / 分析类

| 路径 | 主题说明 |
|---|---|
| `plans/competition-mechanism-assessment.md` | 并发/竞争机制评估 |
| `plans/data-flow-analysis.md` | 数据流分析 |
| `plans/database-cleanup-plan.md` | 数据库目录清理规划 |
| `plans/database-design-analysis-report.md` | 数据库设计分析报告 |
| `plans/database-features-code-collection.md` | 数据库特性代码整理 |
| `plans/database-features-enhancement-plan.md` | 数据库能力增强规划 |
| `plans/database-principles-analysis.md` | 数据库原理映射分析 |
| `plans/database-scripts-analysis-report.md` | 数据脚本分析报告 |
| `plans/database-scripts-cleanup-plan.md` | 数据脚本清理规划 |

#### 重构 / 文件整理类

| 路径 | 主题说明 |
|---|---|
| `plans/enhanced-files-analysis.md` | enhanced 类文件分析 |
| `plans/enhanced-files-refactor-plan.md` | enhanced 类文件重构计划 |
| `plans/enhanced-files-refactor-plan-v2.md` | 重构计划第二版 |

#### UI / 主题 / 美化类

| 路径 | 主题说明 |
|---|---|
| `plans/gdut-theming-plan.md` | 主题配色和广工元素规划 |
| `plans/gui-beautification-plan.md` | UI 美化规划 |

#### 环境 / 安装类

| 路径 | 主题说明 |
|---|---|
| `plans/install-script-redesign-plan.md` | 安装脚本重设计 |
| `plans/nodejs-version-fix-plan.md` | Node 版本与 `better-sqlite3` 兼容问题 |

> `plans/` 目录建议统一归类为：**AI/协作过程中沉淀的规划分析文档，不参与运行**。

---

## 8. 数据、构建产物、素材、本地状态

### 8.1 当前数据与日志

| 路径 | 作用说明 |
|---|---|
| `data/library.db` | 当前 B/S 版实际数据库文件 |
| `logs/audit.log` | 当前运行日志 / 审计日志 |

### 8.2 当前静态资源

| 路径 | 作用说明 |
|---|---|
| `public/images/gdut-logo.jpg` | 当前 Web 版静态资源 |

### 8.3 构建产物

| 路径 | 作用说明 |
|---|---|
| `dist/index.html` | 当前 Web 前端构建入口 |
| `dist/assets/*` | 当前 Web 前端构建后的 JS/CSS/SourceMap |
| `dist/images/gdut-logo.jpg` | 当前 Web 构建后复制出的图片资源 |
| `dist-renderer/index.html` | 旧 Electron 渲染端构建入口 |
| `dist-renderer/assets/*` | 旧 Electron 渲染端构建产物 |
| `dist-renderer/images/gdut-logo.jpg` | 旧渲染端构建后的图片资源 |
| `dist-electron/main/*` | 旧 Electron 主进程构建产物 |
| `dist-electron/preload/*` | 旧 Electron preload 构建产物 |

### 8.4 截图与展示素材

- `pic/*.png`：全部为界面展示截图，主要用于报告、答辩、展示材料。
- `pic.7z`：`pic/` 的压缩备份。

### 8.5 本地状态文件

| 路径 | 作用说明 |
|---|---|
| `.claude/settings.local.json` | 本地 AI 工具权限配置 |
| `.vs/ProjectSettings.json` | IDE 项目状态 |
| `.vs/VSWorkspaceState.json` | IDE 工作区展开状态 |
| `.vs/slnx.sqlite` | IDE 本地数据库/索引文件 |
| `.vs/**/*.wsuo` | IDE 用户状态缓存 |
| `.vs/**/DocumentLayout.json` | IDE 文档布局状态 |

---

## 9. 最容易误判成“冗余”的地方

### 9.1 实际上不能随便删的

- `src/`：虽然不是当前浏览器版运行主线，但它是 **Electron 旧实现参考库**
- `plans/`：虽然不运行，但属于 **问题分析和设计过程文档**
- `docs/`、`pdf/docx`、`pic/`：属于 **比赛/课程提交材料**
- `server/src/domains/note/*` + `web/src/views/Notes.vue`：是 **当前 Web 版有效功能**

### 9.2 最像后续“可归档候选”的

- `web/src/**/*.js`
- `web/src/**/*.js.map`
- `dist/`
- `dist-renderer/`
- `dist-electron/`
- `scripts/archive/*`
- `$null`

> 但在当前多人同时修改的情况下，**先不要移动、删除、重命名这些文件**。

---

## 10. 整理时的安全边界

### 10.1 当前运行最关键的主线

如果只想保住“当前浏览器版能跑起来”的核心，应重点关注：

- `package.json`
- `start.bat`
- `start.sh`
- `web/`
- `server/`
- `data/library.db`
- `public/`

### 10.2 当前不要误操作的内容

- 不要把 `src/` 当成“单纯重复代码”直接删掉
- 不要把 `plans/`、`docs/`、`pic/` 当成“杂项垃圾”处理掉
- 不要把根目录 `index.html` 和 `web/index.html` 混为一谈  
  - `web/index.html`：当前前端入口  
  - 根目录 `index.html`：旧 Electron 入口
- 不要把 `scripts/generate-data.ts`、`scripts/clear-database.ts` 当成当前 Web 版唯一数据脚本  
  它们更偏旧 Electron 路线
- 不要把 `tests/` 当成当前 `server/` 的完整测试体系  
  现在测试更偏旧 Electron 代码

---

## 11. 推荐的归档口径

后续如果你要做人工归档，建议先按下面 6 类贴标签：

1. **当前运行主线**：`web/`、`server/`、`data/`、`public/`、当前启动脚本  
2. **旧 Electron 参考线**：`src/`、根目录 `index.html`、旧 Electron 脚本、打包工具压缩包  
3. **文档与比赛材料**：`docs/`、`plans/`、`README.md`、`DEPLOYMENT.md`、`CLAUDE.md`、`pdf/docx/pic`  
4. **脚本与测试**：`scripts/`、`tests/`、`backup/`  
5. **构建产物**：`dist/`、`dist-renderer/`、`dist-electron/`  
6. **本地状态与第三方工具**：`.vs/`、`.claude/`、`node_modules/`、`logs/`、`ngrok.exe`

---

## 12. 最后说明

- 本说明文件是基于当前仓库状态做的“结构归类快照”。
- 它的目的是帮助你**后续人工整理**，不是替你执行删除、移动、合并。
- 如果后面你想继续细化，我建议下一步单独再做一份：
  - **《可疑重复文件清单》**：只列可能重复但暂时不动的文件
  - **《当前运行最小集清单》**：只列浏览器版真正最小必需文件

---

## 13. 当前 Git 变更状态（本次检查）

### 13.1 检查方式

本次按当前仓库执行了以下检查：

- `git status --short`
- `git status --porcelain=v2 --untracked-files=all`
- `git diff --name-status HEAD -- .`

### 13.2 检查结果

截至本次检查，`git` 工作区**没有发现新的已修改文件、已暂存文件或未跟踪文件**，也就是说：

- 当前没有新的 Git 改动项需要补充到“新增/修改文件清单”
- 现阶段这份 `项目说明.md` 主要仍是**结构归类清单**

### 13.3 后续使用说明

如果之后还有其他 AI 或人工继续改动仓库，建议重新执行一次：

- `git status --short`

然后把新出现的文件路径补到本说明文件，或单独维护一份：

- `当前Git变更清单.md`
