# Node.js 版本管理说明

## ⚠️ 重要提示

本项目使用 **better-sqlite3** 原生模块，**不支持 Node.js 24+**。

推荐使用 **Node.js 20 LTS** 版本。

## 🔧 快速切换（仅项目内生效，不影响全局）

项目已配置 `.nvmrc` 文件，您只需在项目目录执行：

```bash
# 方式1：使用 nvm（推荐）
nvm use

# 方式2：如果没有安装 Node 20，先安装
nvm install 20
nvm use
```

## 📦 安装依赖

切换到 Node 20 后，安装项目依赖：

```bash
npm install
```

## ❓ 如果没有安装 nvm

### Windows 用户

下载安装 [nvm-windows](https://github.com/coreybutler/nvm-windows/releases)：

1. 下载 `nvm-setup.exe`
2. 安装后重启终端
3. 执行 `nvm install 20`
4. 在项目目录执行 `nvm use`

### macOS/Linux 用户

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重启终端后
nvm install 20
nvm use
```

## 🎯 支持的版本

- ✅ **Node.js 18.x** - 支持
- ✅ **Node.js 20.x** - 推荐（LTS）
- ✅ **Node.js 22.x** - 支持
- ❌ **Node.js 24.x** - 不支持（better-sqlite3 编译失败）

## 🔍 验证当前版本

```bash
node --version
# 应该显示 v20.x.x
```

## 💡 原因说明

**better-sqlite3** 是 SQLite 的原生 Node.js 绑定，需要在安装时编译 C++ 代码。Node.js 24 引入了 C++20 要求，但 better-sqlite3 目前的构建配置不兼容这个版本。

使用 `.nvmrc` 可以让您在不同项目间轻松切换 Node 版本，互不影响。
