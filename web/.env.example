# API Base URL
# For local development with Vite proxy (recommended):
VITE_API_BASE_URL=/api/v1

# If frontend and backend are deployed to different origins:
# VITE_API_BASE_URL=https://api.your-server.com/api/v1

# If frontend and backend share the same domain / reverse proxy, keep `/api/v1`.
