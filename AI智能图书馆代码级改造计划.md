# AI 智能图书馆代码级改造计划

## 1. 改造目标

本次改造将项目从“带 AI 助手的图书管理系统”收束为“**面向校园实体书流转的 AI 智能图书馆**”。

核心边界统一如下：

- 学生 / 教师账号：以 **AI 对话、找书、推荐、预约、借阅查询、归还后传承笔记** 为主。
- 机器账号：负责 **图书馆内实体书借还**，以 **读者编号 + 单册条码** 为核心流程。
- 管理员 / 馆员账号：保留后台管理、统计分析、人工借还兜底。
- AI 不再直接“线上借出实体书”，只负责 **推荐、预约、解释、查询**。

## 2. 本轮改造重点

### 2.1 角色与入口

- 新增 `machine` 账号类型。
- 学生 / 教师登录默认首页改为 `AI 智能图书馆` 对话页。
- 机器账号登录默认进入 `自助借还终端`。
- 管理员 / 馆员仍保留后台入口。

### 2.2 实体书借还主链路

- 新增 `book_copies`，实现 **单册条码级借还**。
- 新增 `borrowing_records.copy_id`，让借阅记录绑定到具体副本。
- 机器终端负责真实借书 / 还书。
- 学生 / 教师端去掉“线上直接借走实体书”的误导性设计。

### 2.3 预约替代线上借阅

- 新增 `reservations` 表与预约接口。
- 图书页与 AI 推荐页统一改为 **预约到馆**。
- AI 工具由 `borrow_book` 改为 `reserve_book`。
- AI 回复中明确提示：**预约成功 ≠ 借阅成功，仍需到馆扫码取书**。

### 2.4 传承笔记收口

- 传承笔记改为：**图书归还后才能写**。
- 每次真实借阅记录最多沉淀一篇传承笔记。
- 普通公开笔记与传承笔记并存。
- 后续可把卡片 / 笔记内容交给 AI 汇总成顶层简介。

## 3. 已执行的代码改动

### 3.1 后端

- 数据库支持 `machine` 角色、`book_copies`、`reservations`、`borrowing_records.copy_id`。
- 自动生成默认机器账号：`machine01 / machine123`。
- 新增机器端接口：
  - `GET /api/v1/machine/reader/:readerNo`
  - `GET /api/v1/machine/copy/:barcode`
  - `POST /api/v1/machine/borrow`
  - `POST /api/v1/machine/return`
- 新增预约接口：
  - `POST /api/v1/reservations`
  - `GET /api/v1/reservations/my`
  - `PUT /api/v1/reservations/:id/cancel`
- 借阅服务重写为：
  - 支持条码级借书 / 还书
  - 借阅时自动绑定副本
  - 借书时自动兑现预约
  - 还书时恢复副本状态
- AI 工具边界重写为：
  - 只允许推荐 / 查询 / 预约
  - 不再允许线上借走实体书
  - 明确歧义书目时必须先确认

### 3.2 前端

- 首页入口按角色动态分流：
  - `admin / librarian -> /dashboard`
  - `teacher / student -> /ai-assistant`
  - `machine -> /machine-terminal`
- 重写学生 / 教师端布局为“AI 图书馆主导航”。
- 重写 `AIAssistant.vue`，默认进入 ChatGPT 式对话页。
- 重写 `Books.vue`，普通读者端统一改为“找书预约”。
- 重写 `Borrowing.vue`：
  - 学生 / 教师仅查看借阅、预约、申请续借
  - 明确提示“归还请到馆在自助终端完成”
  - 馆员保留人工借还兜底
- 重写 `Notes.vue`：
  - 传承笔记只能关联已归还图书
  - 长廊页单独查看前人传承
- 新增 `MachineTerminal.vue` 自助借还终端。

## 4. 当前创新点如何表达

### 4.1 已落到主链路的创新点

- **AI 智能图书馆首页**：学生 / 教师登录即进入对话式入口，而不是传统控制台。
- **实体书借还与 AI 分层**：把“咨询 / 推荐 / 预约”和“真实借还”拆给不同角色与场景，避免设计尴尬。
- **单册条码级借还**：让系统从“书目级”走向“副本级”。
- **归还后传承**：笔记不再悬空，而是绑定真实借阅闭环。

### 4.2 下一阶段可直接写进答辩方案的增强点

- **Chat 功能拓展类**
  - 反热门推荐 Agent：不只推热门书，而是结合读者需求推荐“冷门但更适配”的书。
  - 联借路径 Agent：根据你的当前借阅与兴趣，给出“下一本该读什么”的路径建议。

- **借阅流程增强类**
  - 书籍数字孪生：每本实体书形成 AI 概览页，整合库存、副本状态、借阅热度、前人传承摘要。
  - 还书访谈 Agent：还书后自动发起轻量访谈，帮助读者快速沉淀传承内容。

## 5. 答辩表达建议

建议统一强调一句话：

> 我们不是做“线上点一下就借走实体书”，而是做“AI 负责理解需求，机器负责实体流转，笔记负责知识传承”的智能图书馆闭环。

这样可以同时回应：

- 服务对象不明确
- 线上 / 线下场景混乱
- AI 借阅结果与真实流程不一致
- 创新点停留在表层 UI，而不是业务闭环

## 6. 本轮结果

本轮已完成：

- 计划书生成并对齐产品边界
- 后端借还 / 预约 / AI 工具边界改造
- 前端 AI 首页、预约页、借阅页、传承笔记页、机器终端改造
- 前后端编译验证通过

后续如果继续推进，优先顺序建议为：

1. 书籍数字孪生详情页  
2. 还书访谈 Agent  
3. 反热门推荐 / 联借路径 Agent  
4. 传承卡片 AI 顶层简介汇总
